# Plant config. oursqld --config examples/node.toml
listen = "127.0.0.1:3307"
name = "alpha"
intensity = 25
# mesh = "127.0.0.1:3401"
# peer = "127.0.0.1:3402"
# admin = "127.0.0.1:3309"

# default_commit = "SOYUZ"   # bare ZAVERSHIT
# rf = 0                     # 0 = every plant
# tls_cert = "plant.crt"
# tls_key = "plant.key"
# tls_ca = "kollektiv.crt"   # mTLS
